Esta clase añande un entoro a un array de la otra clase
Runnable.

Empieza implementano la interfaz Runnable,

comenzando hay 2 constantes
Una de simpleArray llamda sharedSimpleAray
De tipo entero llamada startValue


despues esta el constructor, para seguir con el
metodo run, que contiene un ciclo, donde el
contador inicia desde la constante startValue y
para despues de 3 numeros adelante de startvalue,
incrementando de 1 en uno, durante este ciclo la variable
del contador en cada iteracion se añade al array